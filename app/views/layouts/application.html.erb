<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="utf-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <meta name="description" content="The largest collection of oil and gas jobs, find companies looking to hire and apply and connect. Join our network today!">
   <%= csrf_meta_tags %>
   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>

 <link rel="shortcut icon" type="image/png" href="<%= asset_path 'favicon.png'%>"/>

  <title>PetroRecruit</title>
</head>
<body>
  <nav>
        <div class="wrapper">
          <a href="<%= root_path %>">
            <%= image_tag "wordlogo.png", id: "logo", alt: "PetroRecruit", width: "320px", height: "50px" %>
          </a>
            <div id="menu">
                &#9776;
            </div>
            <div id="navbtns">
                <% if user_signed_in? %>
                  <a class="btn btn-default btn-lg"  style="bottom: 15px;"  href="<%=destroy_user_session_path%>" rel="nofollow" data-method="delete">Logout</a>
                  <a class="btn btn-primary btn-lg"  style="bottom: 15px;"  href="<%= edit_user_registration_path%>">Profile</a>
                <% else %>
                <%= link_to "Log in", new_user_session_path, class: "btn btn-default btn-lg", style: "bottom: 15px;" %>
                <a class="btn btn-primary btn-lg" style="bottom: 15px;" href="<%= new_user_registration_path%>">Sign Up</a>
                <% end %>
                <a id="orange" href="/">Post a job</a>

                <a id="orange" href="<%=companies_path()%>">Companies</a>
                <a id="orange" href="<%=jobs_path%>">Find A Job</a>

            </div>
        </div>
    </nav>


<div id="top">

  <% flash.each do |key, value| %>
    <div class="alert <%= key %> overlay">
      <%= value %>
    </div>
  <% end %>

  <%= yield %>
</div>
  <div id="footer">
      <div class="wrapper">
        <div class="container">
          <span id="created"><a id="orange" href="mailto:contact@petrorecruit.ca">Contact Us</a> | PetroRecruit ©</span>
        </div>
      </div>
  </div>
</body>
</html>

<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>

<script src="https://cdn.smooch.io/smooch.min.js"></script>

<script>
    Smooch.init({appToken: '4ihr4lgh26bf7pcm7qf3dclka'}).then(function() {
      Smooch.updateUser({
        givenName: '<%= user_signed_in? ? "User ID:#{current_user.id}" : "ANONYMOUS" %>',
        email: '<%= user_signed_in? ? current_user.email : "ANONYMOUS" %>'
      })
    });
</script>
